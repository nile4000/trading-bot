package ch.lueem.tradingbot.adapters.config;

/**
 * Describes which application entry flow should be started from the loaded configuration.
 */
public enum ApplicationMode {
    BACKTEST,
    PAPER
}
