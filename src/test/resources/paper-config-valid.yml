mode: PAPER

paper:
  bot:
    botId: btcusdt-paper-demo
    botVersion: v1
    symbol: BTCUSDT
    timeframe: 1m
  execution:
    exchange: BINANCE_SPOT_DEMO
    orderMode: VALIDATE_ONLY
    tickIntervalMillis: 10000
    initialCash: 1000.0
    orderQuantity: 0.0001
    placeOrdersEnabled: false
    maxOrderNotional: 25.0
  strategy:
    name: queued_actions
    actions:
    - BUY
    - HOLD
    - SELL
  binance:
    apiKeyEnv: BINANCE_DEMO_API_KEY
    secretKeyEnv: BINANCE_DEMO_SECRET_KEY
    recvWindowMillis: 15000

reporting:
  prettyPrint: true
  includeNotes: true

logging:
  lifecycleEvents: false
